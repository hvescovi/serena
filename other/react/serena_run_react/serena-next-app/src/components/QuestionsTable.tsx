import React from 'react';

const QuestionsTable: React.FC = () => {
    return (
        <div id="tabela_questoes" className="container-fluid">
            {/* Area for dynamically displaying questions */}
        </div>
    );
};

export default QuestionsTable;